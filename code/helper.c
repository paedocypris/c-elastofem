#include "helper.h"

extern inline int floatIsZero(double x);
extern inline void *sMalloc(size_t memsize);
extern inline void *sCalloc(size_t count, size_t size);
extern inline void *sRealloc(void *block, size_t memsize);